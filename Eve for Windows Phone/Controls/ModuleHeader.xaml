<UserControl x:Class="EveWindowsPhone.Controls.ModuleHeader"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	FontFamily="{StaticResource PhoneFontFamilyNormal}"
	FontSize="{StaticResource PhoneFontSizeNormal}"
	Foreground="{StaticResource PhoneForegroundBrush}"
	d:DesignHeight="480" d:DesignWidth="480" 
	DataContext="{Binding RelativeSource={RelativeSource Self}}">

	<!-- Header -->
	<StackPanel Orientation="Horizontal" Background="{StaticResource PhoneAccentBrush}">
		<Image Source="{Binding ImageSourcePath}" Height="64" Margin="10,5,5,5" />
		<TextBlock Text="{Binding Alias}" Foreground="White" FontSize="{StaticResource PhoneFontSizeLarge}" VerticalAlignment="Center" />
	</StackPanel>
</UserControl>