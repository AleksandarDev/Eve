module Eve.API.Core {
	// Class that contains all methods required for communication
	// with server side WCF service using WebSockets
	class Communication {
		private ConnectToWebSocket(host: string, auth: string) {

		}
	}
}